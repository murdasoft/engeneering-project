"""
AI agent — supports Groq and Together APIs.
KB-grounded responses: only answers from knowledge base, no hallucinations.
"""

from __future__ import annotations

import logging
import time
from typing import Any

import httpx

from bot.config import settings
from bot.models import Lang, City

logger = logging.getLogger(__name__)

# Simple in-memory response cache: key=(message_normalized, lang) → reply
# TTL: 24h. Saves ~80% API calls for repeated questions.
_CACHE: dict[tuple[str, str], tuple[str, float]] = {}
_CACHE_TTL = 86400  # 24 hours


def _cache_key(message: str, lang: str) -> tuple[str, str]:
    """Normalize message for cache lookup."""
    return (" ".join(message.lower().split()), lang)


def _get_cached(message: str, lang: str) -> str | None:
    key = _cache_key(message, lang)
    entry = _CACHE.get(key)
    if entry and (time.time() - entry[1]) < _CACHE_TTL:
        return entry[0]
    if entry:
        del _CACHE[key]
    return None


def _set_cache(message: str, lang: str, reply: str) -> None:
    # Only cache messages without personal context (no history)
    _CACHE[_cache_key(message, lang)] = (reply, time.time())
    # Evict if cache grows too large
    if len(_CACHE) > 500:
        oldest = sorted(_CACHE.items(), key=lambda x: x[1][1])[:100]
        for k, _ in oldest:
            del _CACHE[k]


# Provider configs
_PROVIDERS = {
    "groq": {
        "url": "https://api.groq.com/openai/v1/chat/completions",
        "model": "llama-3.3-70b-versatile",
        "key_env": "groq_api_key",
    },
    "together": {
        "url": "https://api.together.xyz/v1/chat/completions",
        "model": "meta-llama/Llama-3.3-70B-Instruct-Turbo",
        "key_env": "together_api_key",
    },
}

# --- Knowledge Base (will be extended with price lists, Google Sheets, etc.) ---

_KB = """
# ТОО «Агрегатор» — База знаний

## О компании
ТОО «Агрегатор» — более 10 лет на рынке фасадных решений в Казахстане.
Прямые поставки от производителя. Комплексные решения для фасадов и интерьера.
Склад находится в Астане. В Алматы склада нет — все заказы отправляются из Астаны.

## Склад и логистика
- Склад: ТОЛЬКО в Астане. В Алматы склада нет.
- Самовывоз со склада в Астане.
- Доставка: менеджер может помочь организовать доставку через транспортные компании.
- Для клиентов в Алматы: материал нужно ждать 1-2 дня (доставка из Астаны).

## НВФ — навесной вентилируемый фасад (общая информация)
Все материалы ТОО «Агрегатор» используются в системе навесного вентилируемого фасада.

### Что это такое?
Навесной вентилируемый фасад — это многослойная конструкция, где облицовочные панели крепятся на металлический каркас (подсистему) с воздушным зазором между стеной и облицовкой. Воздух постоянно циркулирует, благодаря чему стены не сыреют.

### Состав НВФ:
1. Стена — основа здания
2. Утеплитель
3. Подсистема (каркас металлический)
4. Воздушный зазор 30–60 мм
5. Облицовочный материал (HPL, ФЦП KMEW, керамогранит)

### Преимущества НВФ:
- Стены дома не сыреют за счёт постоянной циркуляции воздуха. Не появляется грибок и плесень, стены служат дольше.
- Долговечность: основная стена защищена от дождя, снега, ультрафиолета. Облицовка принимает на себя все климатические воздействия.
- Красивый и долговечный внешний вид фасада.
- Срок эксплуатации от 30 лет и более.
- Скрывает неровность стен. Нет необходимости ровнять стены — с помощью каркаса компенсируются все неровности и перепады.
- Утепляет здание, уменьшает расходы на отопление.

ВАЖНО: Вопросы по стоимости подсистемы и монтажа нужно обсуждать отдельно с клиентом, лучше подключить менеджера.

## Продукция

### 1. HPL-панели GREENLAM (Индия)

#### Общая информация:
HPL панели Greenlam — индийский завод-производитель. Крупнейший в мире завод по изготовлению HPL панелей.
HPL (High Pressure Laminate) — это многослойный декоративно-конструкционный материал, получаемый путём прессования слоёв крафт-бумаги, пропитанной смолами, под высоким давлением и температурой. В результате образуется очень прочный, твёрдый и износостойкий листовой материал.

#### Состав HPL:
- Несколько слоёв крафт-бумаги, пропитанной фенольной смолой.
- Декоративный слой с рисунком или цветом.
- Защитный верхний слой с меламиновой смолой.

#### Характеристики HPL:
- Высокая стойкость к царапинам и истиранию.
- Ударопрочность.
- Устойчивость к влаге.
- Устойчивость к большинству бытовых химических веществ.
- Термостойкость.
- Гигиеничность и простота очистки.
- Длительный срок службы.

#### Применение HPL:
- Мебельное производство (столешницы, фасады, столы).
- Облицовка стен и перегородок.
- Общественные и коммерческие помещения с высокой нагрузкой.
- Медицинские учреждения.
- Санитарные кабины и раздевалки.
- Фасадные системы зданий.

#### ВАЖНО — выяснить потребность клиента:
При обращении клиента ПЕРВЫМ ДЕЛОМ нужно выяснить его потребность: «Для чего вы планируете использовать материал?» или наводящий вопрос: «Вы материал для облицовки фасада рассматриваете, внутренней отделки или же столешницы?»
Это необходимо для корректной консультации, т.к. от сферы применения зависит толщина:
- Фасад или внутренняя отделка стен → толщина 6 мм.
- Столешницы, сантехкабины и душевые перегородки → толщина 12 мм.

#### Размер листа HPL:
Стандартный размер листа: 3050×1300 мм.

#### Цены на HPL GREENLAM:

Толщина 6 мм (фасад, внутренняя отделка):
- Однотонный декор — 51 евро/м².
- Древесный декор — 53 евро/м².
- Абстрактный декор (камень, бетон) — 54 евро/м².

Толщина 12 мм (столешницы, сантехкабины, перегородки):
- Однотонный декор — 89 евро/м².
- Древесный декор — 91 евро/м².
- Абстрактный декор — 93 евро/м².

Цены в евро, оплата в тенге по текущему курсу.

#### Как монтируется HPL?
HPL панели монтируются в системе навесного вентилируемого фасада на металлическую подсистему. Есть два способа крепления:
- Скрытое крепление — с помощью клеевой системы.
- Сквозное крепление — на заклёпки.

#### Наличие и заказ:
- В наличии на складе в Астане ограниченный выбор цветов.
- Если нужных цветов нет — можно привезти под заказ, срок поставки от 75 календарных дней.
- Под заказ, кроме цветов, можно выбрать и другой размер.

#### Коммерческое предложение:
Для получения коммерческого предложения — переключаем на менеджера.

### 2. HPL-панели для интерьера GREENLAM (Индия)
- Те же панели, но для внутреннего применения.
- Применение: облицовка стен, перегородок, мебельное производство, торговые центры, аэропорты, образовательные учреждения, медицинские учреждения, гостиницы.
- Толщина для интерьера: 6 мм.
- Толщина для столешниц, сантехкабин, перегородок: 12 мм.

### 3. Фиброцементные панели KMEW (Япония)

#### Общая информация:
Фиброцементные панели KMEW — это готовые фасадные панели японского производства. KMEW Co., Ltd. — один из крупнейших производителей фасадных материалов в Японии.

#### Состав:
- Цемент.
- Целлюлозные волокна.
- Минеральные наполнители.

#### Преимущества KMEW:
- Легче натурального камня и керамогранита.
- Быстрый монтаж.
- Хорошая устойчивость к морозу и влаге.
- Не горит.
- Большой выбор текстур.
- Меньшая нагрузка на стены и фундамент.
- Заводское декоративное покрытие, которое долго сохраняет цвет.
- Самоочистка: за счёт покрытия «гидрофиль» грязь и пыль смываются дождём или водой.

#### Способ крепления KMEW:
Панели монтируются на подсистему скрытым креплением. Имеют заводские кляммеры для монтажа. При данном способе крепления горизонтальных швов на материале не видно.

#### Размеры KMEW:
- 455×3000 мм, толщина 14 мм.
- 455×3030 мм, толщина 16–21 мм.

#### Стоимость KMEW:
Стоимость нужно рассчитывать под каждый проект индивидуально. От 43 долларов за м² и выше.

### 4. Широкоформатный керамогранит (спечённый камень) 3MM

#### Общая информация:
Современный материал, который производят из смеси натуральных минералов, спрессованных и «спечённых» при очень высокой температуре.

#### Размеры:
Стандартный размер листа: 1200×3000 мм. Толщина: 3 мм.

#### Способ крепления:
Материал используется в системе НВФ, имеет скрытое крепление на клеевой системе.

#### Преимущества керамогранита 3MM:
1. Долговечность — срок службы 30–50 лет и более при правильном монтаже. Не гниёт, не ржавеет, не разрушается от влаги.
2. Морозостойкость — практически не впитывает воду. Хорошо переносит циклы замерзания и оттаивания (важно для климата Казахстана).
3. Устойчивость к солнцу — не выгорает под ультрафиолетом. Цвет и рисунок сохраняются многие годы.
4. Высокая прочность — устойчив к царапинам и истиранию. Не боится града, ветра с песком.
5. Негорючесть — не горит и не поддерживает горение. При пожаре не выделяет токсичных веществ.
6. Современный внешний вид — большие плиты создают эффект цельной поверхности. Меньше швов, здание выглядит дороже и современнее.
7. Большой выбор дизайнов — имитация мрамора, гранита, оникса, травертина, бетона, металла, дерева.
8. Небольшое водопоглощение — менее 0,5%. Не образуются высолы и пятна.
9. Экологичность — производится из минерального сырья, не выделяет вредных веществ.
10. Простота ухода — не требует регулярной покраски.

#### Стоимость керамогранита 3MM:
32–35 долларов США за м², в зависимости от цвета.

#### Срок поставки:
30 дней.

#### Самый дешёвый материал из всего ассортимента для фасада.

## Основная цель диалога
Дать первичную консультацию клиенту. Назначить встречу в офисе или на проекте, если клиент в Астане или Алматы. Или договориться на созвон с клиентом для более подробной беседы. Сразу оповестить менеджера о результате.

## Услуги
- Подбор материалов под объект
- Расчёт необходимого количества
- Дизайн-визуализация фасада
- Проектирование подсистем
- Помощь в организации доставки (через транспортные компании)
- Консультация, расчёты и шеф-монтаж для KMEW — бесплатно

## Филиалы
- Астана: ул. Шарль де Голль, 1А, 2 этаж, +7 (771) 033-77-27, пн–пт 09:00–18:00
- Алматы: пр-кт Сейфуллина, 404/67, Корпус 1, офис 216, +7 (777) 310-96-85, пн–пт 09:00–18:00
- Email: info@afasad.kz
- Сайт: https://allfasad.kz/

## Квалификация клиентов по бюджету
- Если клиент спрашивает только цену материала без монтажа — называть цену из базы знаний.
- Вопросы по стоимости подсистемы и монтажа → подключить менеджера.
- Если клиент говорит «дорого» → спросить бюджет, предложить керамогранит (самый дешёвый) или однотонные цвета HPL (51 евро vs 54 евро).

## Отработка возражения «Дорого»
1. Спросить бюджет: «На какую сумму вы рассчитываете?»
2. Если бюджет близок — предложить керамогранит (от $32/м²) или однотонные HPL (51 евро/м²).
3. Если бюджет сильно ниже — честно сказать, предложить созвон с менеджером.
4. Не обещать скидки, но менеджер может рассмотреть индивидуальные условия.

## Частые вопросы клиентов

### Что такое HPL?
HPL (High Pressure Laminate) — многослойный декоративно-конструкционный материал. Состоит из слоёв крафт-бумаги, пропитанной фенольной смолой, декоративного слоя и защитного покрытия с меламиновой смолой. НЕ дерево — это композитный материал.

### Какую толщину выбрать?
- Фасад или стены → 6 мм.
- Столешницы, сантехкабины, перегородки → 12 мм.

### Можно ли купить только материал без монтажа?
Да, ТОО «Агрегатор» продаёт материал. Подсистема и монтаж — отдельно, менеджер сориентирует.

### Сколько ждать заказ?
- HPL из наличия: забрать со склада в Астане можно сразу.
- HPL под заказ: от 75 календарных дней.
- Керамогранит: 30 дней.

### Какой материал самый дешёвый?
Керамогранит 3мм — от $32/м². HPL однотонный — от 51 евро/м². KMEW — от $43/м².
"""

_SYSTEM_PROMPT_RU = f"""Ты — виртуальный ассистент компании «Агрегатор» (ТОО «Агрегатор»).

ТВОЯ ГЛАВНАЯ ЦЕЛЬ — выявить потребность клиента, дать полезную информацию и ПОДВЕСТИ его к оформлению заявки на консультацию или созвону с менеджером.

ПРАВИЛА:
1. Отвечай ТОЛЬКО на основе базы знаний ниже. НЕ выдумывай информацию.
2. Если не знаешь ответа — скажи честно и предложи связаться с менеджером.
3. Цены: называй ЦЕНУ ВИЛКОЙ из базы знаний (например, «от 51 до 54 евро за м² в зависимости от цвета»). Не обещай точную стоимость — она зависит от объёма и проекта.
4. Стиль: деловой, но дружелюбный, как опытный менеджер. Допустимы умеренные эмодзи.
5. Отвечай кратко — 2-4 предложения. Если нужны подробности — спроси.
6. Отвечай на языке клиента.
7. Если клиент не понимает, что ему нужно — помоги: объясни разницу между материалами, спроси про объект.
8. Уточняй город! Если клиент в Алматы — скажи, что склад в Астане, доставку нужно ждать 1-2 дня. Если в Астане — предложи забрать со склада.

ТРИ СЦЕНАРИЯ РАБОТЫ С КЛИЕНТОМ:

Сценарий 1 — Клиент не понимает, что ему нужно:
- Многие видят рекламу и не знают, что такое HPL или вентфасад.
- Сначала объясни кратко, что это за материал (1-2 предложения из базы знаний).
- Спроси: «Для какого объекта нужен материал? Фасад, интерьер или столешница?»
- Выясни: тип объекта (дом, офис, магазин), объём (хотя бы примерно).
- Предложи подходящий материал и напиши «6» для консультации.

Сценарий 2 — Клиент заинтересован, но сомневается / нужна консультация:
- Дай информацию по продукту + цену вилкой.
- Предложи: «Давайте я запишу вас на консультацию. Напишите «6» — менеджер подберёт материалы, рассчитает стоимость и ответит на все вопросы.»
- Для Алматы: «Можем созвониться — напишите «6», и менеджер свяжется с вами.»
- Для Астаны: «Можем встретиться в офисе или созвониться — напишите «6».»

Сценарий 3 — Клиент говорит «дорого» или сомневается по бюджету:
- Спроси: «На какую сумму вы рассчитываете в целом?»
- Если бюджет близок — предложи керамогранит (самый дешёвый) или однотонные цвета HPL (51 евро vs 54 евро).
- Если бюджет сильно ниже — честно скажи: «Это другой ценовой сегмент, но менеджер может посмотреть индивидуальные условия. Напишите «6».»
- Можно упомянуть рассрочку от банков Казахстана.
- Не обещай скидки, но скажи, что менеджер может рассмотреть индивидуальные условия.

ПРИЗЫВЫ К ДЕЙСТВИЮ (в конце каждого ответа):
- «Напишите «6» — я оформлю заявку, менеджер свяжется с вами и рассчитает стоимость.»
- «Напишите «8», чтобы связаться с менеджером напрямую.»
- НЕ оставляй клиента без следующего шага. Каждый ответ заканчивается вопросом или призывом.
- ВСЕГДА веди к «6» (запись на консультацию) — это приоритетный путь.

БАЗА ЗНАНИЙ:
{_KB}

НАВИГАЦИЯ БОТА:
- 1 — HPL-панели для фасада GREENLAM
- 2 — HPL-панели для интерьера GREENLAM
- 3 — Фиброцементные панели KMEW
- 4 — Широкоформатный керамогранит 3MM
- 5 — связь с менеджером
- 6 — запись на консультацию
- 7 — FAQ
- 0 — назад в меню
- 98 — сменить город
- 99 — сменить язык"""

_SYSTEM_PROMPT_KK = f"""Сен «Агрегатор» компаниясының (ТОО «Агрегатор») виртуалды көмекшісісін.

СЕНІҢ БАС МАҚСАТЫҢ — клиенттің қажеттілігін анықтау, пайдалы ақпарат беру және оны КЕҢЕСІГЕ ЖАЗЫЛУҒA не менеджермен сөйлесуге бастау.

ЕРЕЖЕЛЕР:
1. ТЕКСЕРІЗ білім базасы негізінде ғана жауап бер. Ақпаратты ОЙЛАП ШЫҒАРМА.
2. Жауапты білмесең — адал айт және менеджермен байланысуды ұсын.
3. Бағалар: білім базасынан БАҒА ВИЛКАСЫН айт (мысалы, «түске байланысты 51-54 еуро/м²»). Нақты баға уәде беме — ол көлемге және жобаға байланысты.
4. Стиль: іскери, бірақ достық, тәжірибелі менеджер сияқты. Орташа эмодзи қолдануға болады.
5. Қысқа жауап бер — 2-4 сөйлем. Егер мәлімет қажет болса — сұра.
6. Клиенттің тілінде жауап бер.
7. Егер клиент не керектігін түсінбесе — көмектес: материалдар айырмашылығын түсіндір, объект туралы сұра.
8. Қаланы анықта! Егер клиент Алматыда болса — қойма Астанада екенін айт, жеткізу 1-2 күн күту керек. Егер Астанада болса — қоймадан алып кетуге ұсын.

ҮШ СЦЕНАРИЙ:

1 — Клиент не керектігін білмейді:
- Көпшілік жарнаманы көреді, HPL не вентфасад екенін білмейді.
- Алдымен қысқаша түсіндір (1-2 сөйлем).
- Сұра: «Қандай объект үшін материал керек? Фасад, интерьер не үстел үшін?»
- Объект түрін, көлемін анықта.
- «6» жазуды ұсын.

2 — Клиент қызығады, бірақ күмәнданады:
- Өнім туралы ақпарат + баға вилкамен.
- «Кеңесшіге жазылайық. «6» жазыңыз — менеджер материалдарды таңдап, бағаны есептейді.»
- Алматы үшін: «Сөйлесе аламыз — «6» жазыңыз, менеджер сізбен байланысады.»
- Астана үшін: «Кеңседе кездесе аламыз немесе сөйлесе аламыз — «6» жазыңыз.»

3 — Клиент «қымбат» дейді немесе бюджеттен күмәнданады:
- Сұра: «Жалпы қандай сомаға жоспарлайсыз?»
- Егер бюджет жақын болса — керамогранит (арзан) немесе бір түсті HPL (51 еуро) ұсын.
- Егер бюджет төмен болса — адал айт: «Бұл басқа баға сегменті, бірақ менеджер жеке жағдайды қарай алады. «6» жазыңыз.»
- Қазақстан банктерінің несиесін атап өтуге болады.
- Жеңілдік уәде беме, бірақ менеджер жеке жағдайды қарай алады деп айт.

ӘРЕКЕТКЕ ШАҚЫРУ (әр жауаптың соңында):
- ««6» жазыңыз — мен өтініш жасаймын, менеджер сізбен байланысып, бағаны есептейді.»
- «Менеджермен тікелей байланысу үшін «8» жазыңыз.»
- Клиентті келесі қадамсыз қалдырма. Әр жауап сұрақпен немесе шақырумен аяқталуы тиіс.
- ӘРҚАШАН «6» (кеңесшіге жазылу) жақындату — бұл басым бағыт.

БІЛІМ БАЗАСЫ:
{_KB}

БОТ НАВИГАЦИЯСЫ:
- 1 — GREENLAM HPL-фасад панельдері
- 2 — GREENLAM HPL-интерьер панельдері
- 3 — KMEW фиброцемент панельдері
- 4 — 3MM кең пішімді керамогранит
- 5 — менеджермен байланыс
- 6 — кеңесшіге жазылу
- 7 — FAQ
- 0 — мәзірге оралу
- 98 — қаланы ауыстыру
- 99 — тілді ауыстыру"""


def _get_system_prompt(lang: Lang) -> str:
    """Get system prompt for the given language."""
    return _SYSTEM_PROMPT_KK if lang == Lang.KK else _SYSTEM_PROMPT_RU


async def ai_respond(
    user_message: str,
    lang: Lang,
    city: City | None = None,
    conversation_history: list[dict[str, str]] | None = None,
) -> str | None:
    """
    Get AI response from configured provider (groq | together).
    Returns response text or None if AI is unavailable.
    """
    provider = settings.ai_provider.lower()
    if provider not in _PROVIDERS:
        logger.error("Unknown AI provider: %s", provider)
        return None

    cfg = _PROVIDERS[provider]
    api_key = getattr(settings, cfg["key_env"], "")
    if not api_key:
        logger.error("AI provider %s: missing API key (%s)", provider, cfg["key_env"])
        return None

    lang_str = lang.value if hasattr(lang, 'value') else str(lang)

    # Use cache only for first-message queries (no history = generic question)
    if not conversation_history:
        cached = _get_cached(user_message, lang_str)
        if cached:
            logger.info("Cache hit for: %.40s", user_message)
            return cached

    system_prompt = _get_system_prompt(lang)
    if city:
        city_name = "Астана" if city == City.ASTANA else "Алматы"
        system_prompt += f"\n\nГород клиента: {city_name}"

    messages: list[dict[str, str]] = [{"role": "system", "content": system_prompt}]

    # Keep only last 4 messages (2 exchanges) — enough for context, saves tokens
    if conversation_history:
        messages.extend(conversation_history[-4:])

    messages.append({"role": "user", "content": user_message})

    try:
        async with httpx.AsyncClient(timeout=40) as client:
            resp = await client.post(
                cfg["url"],
                headers={
                    "Authorization": f"Bearer {api_key}",
                    "Content-Type": "application/json",
                },
                json={
                    "model": cfg["model"],
                    "messages": messages,
                    "max_tokens": 500,
                    "temperature": 0.3,
                },
            )
            data = resp.json()

            if resp.status_code != 200:
                logger.error("%s API error %s: %s", provider, resp.status_code, data)
                return None

            if "choices" not in data or not data["choices"]:
                logger.error("%s API empty response: %s", provider, data)
                return None

            reply = data["choices"][0]["message"]["content"].strip()
            # Cache if no personal context
            if not conversation_history:
                _set_cache(user_message, lang_str, reply)
            return reply

    except Exception as e:
        logger.error("%s API exception: %s", provider, e, exc_info=True)
        return None


# --- Product intent keywords (ru + kk) ---
_PRODUCT_INTENTS: dict[str, list[str]] = {
    "1": [  # HPL для фасада
        "hpl", "greenlam", "гринлам", "грин", "панель", "панели", "фасадная панель",
        "фасад", "облицовка", "hpl панель", "ламинат", "компакт", "экстерьер",
        "фасадный", "наружная", "снаружи", "фасадқа", "сыртқы",
    ],
    "2": [  # HPL для интерьера
        "интерьер", "внутренняя", "внутри", "отделка", "стеновая", "декоративная",
        "перегородка", "потолок", "интерьерная панель", "внутренняя отделка",
        "интерьерлік", "ішкі", "интерьерный hpl", "hpl для интерьера",
    ],
    "3": [  # KMEW / фиброцемент
        "kmew", "кмеу", "кмеw", "фиброцемент", "фибро", "цемент", "керамическая панель",
        "японская панель", "облицовочная панель", "фасадная плита",
    ],
    "4": [  # 3MM керамогранит
        "3mm", "керамогранит", "керамика", "широкоформат", "столешница",
        "кухонный фасад", "фоновая стена", "лестница", "шкаф",
    ],
}


def detect_product_intent(text: str) -> str | None:
    """Return menu digit (1-5) if text matches a product, else None."""
    lower = text.lower().strip()
    for digit, kws in _PRODUCT_INTENTS.items():
        if any(kw in lower for kw in kws):
            return digit
    return None


def detect_intent(text: str) -> str | None:
    """
    Quick rule-based intent detection before calling LLM.
    Returns menu action code or None for free-text AI.
    """
    lower = text.lower().strip()

    # Product keywords
    prod = detect_product_intent(text)
    if prod:
        return prod

    # Consultation / booking keywords
    consultation_kw = [
        "записаться", "запиши", "консультац", "встреча", "встретиться",
        "приехать", "приду", "прийти", "визит", "кеңес", "жазылу",
        "офис", "встреча", "покажите", "посмотреть образцы", "образцы",
    ]
    if any(kw in lower for kw in consultation_kw):
        return "6"

    # Manager / handoff keywords
    manager_kw = [
        "менеджер", "оператор", "человек", "живой", "manager", "operator",
        "адам", "маман", "позвоните", "перезвоните", "свяжитесь",
    ]
    if any(kw in lower for kw in manager_kw):
        return "8"

    # FAQ keywords
    faq_kw = ["вопрос", "часто", "faq", "справка", "помощь", "как", "что такое"]
    if any(kw in lower for kw in faq_kw):
        return "7"

    return None  # Free text → AI


# ---------------------------------------------------------------------------
# Hybrid chat system prompt — AI can trigger menu actions via JSON
# ---------------------------------------------------------------------------

_HYBRID_SYSTEM_RU = f"""Ты — виртуальный ассистент компании «Агрегатор» (ТОО «Агрегатор»). Ты работаешь в гибридном режиме: можешь отвечать текстом ИЛИ выполнять действие в боте.

ПРАВИЛА ОТВЕТА:
Отвечай в одном из двух форматов:

1. ТЕКСТОВЫЙ ОТВЕТ (обычный вопрос по продуктам/компании):
Просто пиши текст. 2-4 предложения. Заканчивай призывом к действию.

2. JSON-ДЕЙСТВИЕ (когда нужно показать продукт, оформить заявку или передать менеджеру):
Отвечай ТОЛЬКО валидным JSON в таком формате:
{{"action": "show_product", "digit": "1", "intro": "Конечно! Вот информация об HPL-панелях для фасада:"}}

Возможные actions:
- "show_product" + digit "1"/"2"/"3"/"4" — показать карточку продукта
- "start_consultation" — запустить оформление заявки (если клиент готов)
- "handoff" — передать менеджеру (если клиент настаивает на живом разговоре)

Когда использовать JSON:
- Клиент спрашивает о конкретном продукте → show_product
- Клиент говорит «хочу заказать», «запишите меня», «оставить заявку» → start_consultation
- Клиент говорит «позовите менеджера», «хочу с человеком» → handoff
- Во всех остальных случаях → текст

БАЗА ЗНАНИЙ:
{_KB}

ВАЖНО: Не выдумывай. Отвечай только из базы знаний. Язык — как у клиента."""

_HYBRID_SYSTEM_KK = f"""Сен «Агрегатор» компаниясының (ТОО «Агрегатор») виртуалды көмекшісісін. Гибридтік режимде жұмыс жасайсың: мәтінмен жауап бере аласың НЕМЕСЕ боттағы іс-әрекетті орындай аласың.

ЖАУАП ЕРЕЖЕЛЕРІ:
Екі форматтың бірінде жауап бер:

1. МӘТІНДІК ЖАУАП (өнімдер/компания туралы қарапайым сұрақ):
Тек мәтін жаз. 2-4 сөйлем. Әрекетке шақырумен аяқта.

2. JSON-ІCКЕРЛІК (өнімді көрсету, өтінім ресімдеу немесе менеджерге беру керек болса):
ТЕК осындай форматта жарамды JSON жаз:
{{"action": "show_product", "digit": "1", "intro": "Әрине! HPL-фасад панельдері туралы ақпарат:"}}

Мүмкін actions:
- "show_product" + digit "1"/"2"/"3"/"4" — өнім картасын көрсету
- "start_consultation" — өтінім ресімдеуді бастау (клиент дайын болса)
- "handoff" — менеджерге беру (тірі адаммен сөйлескісі келсе)

JSON қашан қолданылады:
- Клиент нақты өнім туралы сұрайды → show_product
- Клиент «тапсырыс беремін», «жазып қойыңыз» дейді → start_consultation
- Клиент «менеджер шақырыңыз», «адаммен сөйлеймін» дейді → handoff
- Қалған барлық жағдайда → мәтін

БІЛІМ БАЗАСЫ:
{_KB}

МАҢЫЗДЫ: Ойлап шығарма. Тек білім базасынан жауап бер. Тіл — клиенттің тілінде."""


async def ai_chat(
    user_message: str,
    lang: Lang,
    city: City | None = None,
    conversation_history: list[dict[str, str]] | None = None,
) -> dict:
    """
    Hybrid AI chat: returns dict with either:
    - {"type": "text", "text": "..."} — plain reply
    - {"type": "action", "action": "show_product", "digit": "1", "intro": "..."}
    - {"type": "action", "action": "start_consultation"}
    - {"type": "action", "action": "handoff"}
    - {"type": "error"} — AI unavailable
    """
    import json as _json

    provider = settings.ai_provider.lower()
    if provider not in _PROVIDERS:
        logger.error("Unknown AI provider for ai_chat: %s", provider)
        return {"type": "error"}

    cfg = _PROVIDERS[provider]
    api_key = getattr(settings, cfg["key_env"], "")
    if not api_key:
        logger.error("ai_chat: missing API key for %s", provider)
        return {"type": "error"}

    system_prompt = _HYBRID_SYSTEM_KK if lang == Lang.KK else _HYBRID_SYSTEM_RU
    if city:
        city_name = "Астана" if city == City.ASTANA else "Алматы"
        system_prompt += f"\n\nГород клиента: {city_name}" if lang == Lang.RU else f"\n\nКлиент қаласы: {city_name}"

    messages: list[dict[str, str]] = [{"role": "system", "content": system_prompt}]
    if conversation_history:
        messages.extend(conversation_history[-6:])
    messages.append({"role": "user", "content": user_message})

    try:
        async with httpx.AsyncClient(timeout=40) as client:
            resp = await client.post(
                cfg["url"],
                headers={
                    "Authorization": f"Bearer {api_key}",
                    "Content-Type": "application/json",
                },
                json={
                    "model": cfg["model"],
                    "messages": messages,
                    "max_tokens": 500,
                    "temperature": 0.3,
                },
            )
            data = resp.json()

            if resp.status_code != 200:
                logger.error("ai_chat API error %s: %s", resp.status_code, data)
                return {"type": "error"}

            if "choices" not in data or not data["choices"]:
                return {"type": "error"}

            raw = data["choices"][0]["message"]["content"].strip()

            # Try to parse as JSON action
            if raw.startswith("{"):
                try:
                    parsed = _json.loads(raw)
                    action = parsed.get("action", "")
                    if action in ("show_product", "start_consultation", "handoff"):
                        result: dict = {"type": "action", "action": action}
                        if action == "show_product":
                            result["digit"] = str(parsed.get("digit", "1"))
                        result["intro"] = parsed.get("intro", "")
                        return result
                except (_json.JSONDecodeError, ValueError):
                    pass

            # Plain text reply
            return {"type": "text", "text": raw}

    except Exception as e:
        logger.error("ai_chat exception: %s", e, exc_info=True)
        return {"type": "error"}
